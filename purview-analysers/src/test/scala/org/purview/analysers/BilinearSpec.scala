package org.purview.analysers

import org.specs.Specification
import org.specs.runner.JUnit4

class BilinearSpecTest extends JUnit4(BilinearSpec)

object BilinearSpec extends Specification {
  "The bilinear resize detection analyser" should {
    "output a result" in {
      
    }
  }
}
